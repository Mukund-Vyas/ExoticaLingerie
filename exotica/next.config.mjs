/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['localhost', 'www.exoticalingerie.in', 'www.api.exoticalingerie.in', 'api.exoticalingerie.in'],
  },
};

export default nextConfig;
